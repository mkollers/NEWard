import { LandingPageRoutingModule } from './landing-page-routing.module';

describe('LandingPageRoutingModule', () => {
    it('should create', () => {
        // Act
        const module = new LandingPageRoutingModule();

        // Assert
        expect(module).toBeTruthy();
    });
});
