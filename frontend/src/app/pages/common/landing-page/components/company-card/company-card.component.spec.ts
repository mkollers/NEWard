import { CompanyCardComponent } from './company-card.component';

describe('CompanyCardComponent', () => {
  it('should create', () => {
    // Act
    const component = new CompanyCardComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
