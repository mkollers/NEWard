import { IntroComponent } from './intro.component';

describe('IntroComponent', () => {
  it('should create', () => {
    // Act
    const component = new IntroComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
