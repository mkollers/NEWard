import { ProductListingComponent } from './product-listing.component';

describe('ProductListingComponent', () => {
  it('should create', () => {
    // Act
    const component = new ProductListingComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
