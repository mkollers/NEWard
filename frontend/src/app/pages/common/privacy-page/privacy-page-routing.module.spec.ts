import { PrivacyPageRoutingModule } from './privacy-page-routing.module';

describe('PrivacyPageRoutingModule', () => {
    it('should create', () => {
        // Act
        const module = new PrivacyPageRoutingModule();

        // Assert
        expect(module).toBeTruthy();
    });
});
