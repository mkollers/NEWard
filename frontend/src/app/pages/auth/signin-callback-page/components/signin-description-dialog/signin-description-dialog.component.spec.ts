import { SigninDescriptionDialogComponent } from './signin-description-dialog.component';

describe('SigninDescriptionDialogComponent', () => {
  it('should create', () => {
    // Act
    const component = new SigninDescriptionDialogComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
