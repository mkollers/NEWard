import { ProductMobileViewComponent } from './product-mobile-view.component';

describe('ProductMobileViewComponent', () => {
  it('should create', () => {
    // Act
    const component = new ProductMobileViewComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
