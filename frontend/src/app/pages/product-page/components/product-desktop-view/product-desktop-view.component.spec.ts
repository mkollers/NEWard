import { ProductDesktopViewComponent } from './product-desktop-view.component';

describe('ProductDesktopViewComponent', () => {
  it('should create', () => {
    // Act
    const component = new ProductDesktopViewComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
