import { ProductPageComponent } from './product-page.component';

describe('ProductPageComponent', () => {
  it('should create', () => {
    // Act
    const component = new ProductPageComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
