import { ProductPageRoutingModule } from './product-page-routing.module';

describe('ProductPageRoutingModule', () => {
    it('should create', () => {
        // Act
        const module = new ProductPageRoutingModule();

        // Assert
        expect(module).toBeTruthy();
    });
});
