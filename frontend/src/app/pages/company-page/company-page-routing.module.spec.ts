import { CompanyPageRoutingModule } from './company-page-routing.module';

describe('CompanyPageRoutingModule', () => {
    it('should create', () => {
        // Act
        const module = new CompanyPageRoutingModule();

        // Assert
        expect(module).toBeTruthy();
    });
});
