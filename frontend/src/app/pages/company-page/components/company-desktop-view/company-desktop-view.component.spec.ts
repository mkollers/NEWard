import { CompanyDesktopViewComponent } from './company-desktop-view.component';

describe('CompanyDesktopViewComponent', () => {
  it('should create', () => {
    // Act
    const component = new CompanyDesktopViewComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
