import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AskForParticipateDialogComponent } from './ask-for-participate-dialog.component';

describe('AskForParticipateDialogComponent', () => {
  // let component: AskForParticipateDialogComponent;
  // let fixture: ComponentFixture<AskForParticipateDialogComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ AskForParticipateDialogComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(AskForParticipateDialogComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
