import { FirstVoteDialogComponent } from './first-vote-dialog.component';

describe('FirstVoteDialogComponent', () => {
  it('should create', () => {
    // Act
    const component = new FirstVoteDialogComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
