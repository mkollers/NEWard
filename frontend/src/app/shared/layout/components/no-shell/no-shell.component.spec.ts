import { NoShellComponent } from './no-shell.component';

describe('NoShellComponent', () => {
  it('should create', () => {
    // Act
    const component = new NoShellComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
